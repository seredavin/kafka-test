# Руководство по внесению вклада

Спасибо за интерес к проекту! Мы рады любому вкладу: исправлениям ошибок, новым функциям, улучшению документации.

## Процесс разработки

### 1. Подготовка окружения

```bash
# Форк и клонирование
git clone https://github.com/YOUR-USERNAME/kafka-test.git
cd kafka-test

# Установка зависимостей
go mod download

# Проверка, что все работает
go test ./...
go build
```

### 2. Создание ветки

Создайте отдельную ветку для ваших изменений:

```bash
git checkout -b feature/your-feature-name
# или
git checkout -b fix/bug-description
```

Используйте префиксы:
- `feature/` - новая функциональность
- `fix/` - исправление ошибок
- `docs/` - изменения в документации
- `refactor/` - рефакторинг без изменения функциональности
- `test/` - добавление или улучшение тестов

### 3. Разработка

#### Стиль кода

- Следуйте [Effective Go](https://golang.org/doc/effective_go.html)
- Используйте `gofmt` для форматирования кода
- Запускайте линтер перед коммитом:
  ```bash
  golangci-lint run
  ```

#### Написание тестов

- ✅ Все новые функции должны иметь тесты
- ✅ Стремитесь к покрытию не менее 80%
- ✅ Тесты должны быть изолированными и детерминированными
- ✅ Используйте table-driven тесты где возможно

Пример:

```go
func TestMyFunction(t *testing.T) {
    tests := []struct {
        name     string
        input    string
        expected string
        wantErr  bool
    }{
        {
            name:     "valid input",
            input:    "test",
            expected: "TEST",
            wantErr:  false,
        },
        {
            name:     "empty input",
            input:    "",
            expected: "",
            wantErr:  true,
        },
    }

    for _, tt := range tests {
        t.Run(tt.name, func(t *testing.T) {
            result, err := MyFunction(tt.input)
            
            if (err != nil) != tt.wantErr {
                t.Errorf("MyFunction() error = %v, wantErr %v", err, tt.wantErr)
                return
            }
            
            if result != tt.expected {
                t.Errorf("MyFunction() = %v, want %v", result, tt.expected)
            }
        })
    }
}
```

#### Документирование кода

- Все экспортируемые функции, типы и константы должны иметь комментарии
- Комментарии должны начинаться с имени объекта
- Используйте полные предложения

```go
// Config представляет конфигурацию подключения к Kafka.
// Она может быть загружена из файла или создана программно.
type Config struct {
    Brokers []string `json:"brokers"`
    Topic   string   `json:"topic"`
}

// LoadConfig загружает конфигурацию из файла в домашней директории пользователя.
// Если файл не существует, возвращается конфигурация по умолчанию.
func LoadConfig() (*Config, error) {
    // ...
}
```

### 4. Коммиты

Используйте осмысленные сообщения коммитов:

```bash
# Хорошо
git commit -m "Add support for SASL authentication"
git commit -m "Fix panic when broker list is empty"
git commit -m "Update README with installation instructions"

# Плохо
git commit -m "fix"
git commit -m "update"
git commit -m "changes"
```

Формат сообщения:

```
<type>: <short summary>

<detailed description (optional)>

<footer (optional)>
```

Типы:
- `feat`: новая функциональность
- `fix`: исправление ошибки
- `docs`: изменения в документации
- `style`: форматирование, пропущенные точки с запятой и т.д.
- `refactor`: рефакторинг кода
- `test`: добавление тестов
- `chore`: обновление зависимостей, конфигурации и т.д.

### 5. Проверка перед отправкой

Перед созданием Pull Request убедитесь:

```bash
# Запустить тесты
go test -v -race ./...

# Проверить покрытие
go test -coverprofile=coverage.out ./...
go tool cover -func=coverage.out

# Запустить линтер
golangci-lint run

# Проверить форматирование
gofmt -s -w .

# Убедиться, что проект собирается
go build
```

### 6. Создание Pull Request

1. Push ветки в ваш форк:
   ```bash
   git push origin feature/your-feature-name
   ```

2. Откройте Pull Request на GitHub

3. Заполните описание PR:
   - Что изменено и почему
   - Как протестировать изменения
   - Ссылки на связанные issue (если есть)

4. Дождитесь прохождения CI checks

5. Отвечайте на комментарии ревьюеров

## Структура проекта

```
kafka-test/
├── .github/
│   └── workflows/        # GitHub Actions workflows
├── main.go              # Точка входа
├── config.go            # Работа с конфигурацией
├── config_test.go       # Тесты конфигурации
├── producer.go          # Kafka producer логика
├── producer_test.go     # Тесты producer
├── ui.go                # TUI интерфейс
├── ui_test.go           # Тесты UI
├── go.mod              # Зависимости
├── README.md           # Основная документация
├── RELEASE.md          # Процесс релизов
└── CONTRIBUTING.md     # Это руководство
```

## Отчет об ошибках

Если вы нашли ошибку, создайте issue со следующей информацией:

- Описание проблемы
- Шаги для воспроизведения
- Ожидаемое поведение
- Фактическое поведение
- Версия Go
- ОС и архитектура
- Версия программы (или коммит)

Пример:

```markdown
**Описание:**
Программа падает при попытке подключения к Kafka с пустым списком brokers.

**Шаги для воспроизведения:**
1. Запустить программу
2. Оставить поле Brokers пустым
3. Нажать F5 (Connect)

**Ожидаемое поведение:**
Должно показываться сообщение об ошибке "Brokers list cannot be empty"

**Фактическое поведение:**
```
panic: runtime error: index out of range [0] with length 0
```

**Окружение:**
- Go version: 1.21.0
- OS: macOS 13.0 (ARM64)
- Version: v1.0.0
```

## Запросы на новые функции

Перед началом работы над новой функцией:

1. Создайте issue с описанием функции
2. Обсудите подход и API
3. Дождитесь одобрения от maintainers
4. Начинайте разработку

Это поможет избежать ситуации, когда PR не может быть принят из-за несоответствия видению проекта.

## Code Review Process

После создания PR:

1. Автоматически запустятся CI checks
2. Maintainer проведет code review
3. Могут потребоваться изменения
4. После одобрения PR будет смержен

Не расстраивайтесь, если потребуются изменения - это нормальная часть процесса!

## Вопросы?

Если у вас есть вопросы:
- Создайте issue с меткой `question`
- Опишите ваш вопрос максимально подробно

## Лицензия

Внося вклад в проект, вы соглашаетесь с тем, что ваш код будет лицензирован под MIT License.

---

Еще раз спасибо за ваш вклад! 🎉

