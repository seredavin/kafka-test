# Release Process

Этот документ описывает процесс создания релизов для Kafka Producer UI.

## Автоматическая сборка релизов

Проект использует GitHub Actions для автоматической сборки и публикации релизов.

## Создание нового релиза

### 1. Подготовка к релизу

Убедитесь, что:
- Все тесты проходят успешно
- Код прошёл code review
- Все изменения закоммичены в ветку `main`/`master`

### 2. Создание тега

```bash
# Получите последнюю версию main
git checkout main
git pull origin main

# Создайте тег (используйте семантическое версионирование)
git tag -a v1.0.0 -m "Release v1.0.0"

# Отправьте тег в репозиторий
git push origin v1.0.0
```

### 3. Автоматическая сборка

После отправки тега GitHub Actions автоматически:

1. ✅ Запустит все тесты
2. 🔨 Соберёт бинарные файлы для всех платформ:
   - Linux (AMD64, ARM64)
   - macOS (AMD64, ARM64)
   - Windows (AMD64, ARM64)
3. 📦 Создаст архивы (tar.gz для Unix, zip для Windows)
4. 🔐 Сгенерирует SHA256 контрольные суммы
5. 📝 Создаст релиз на GitHub с артефактами

### 4. Проверка релиза

После завершения workflow проверьте:
- Релиз создан на странице [Releases](../../releases)
- Все артефакты загружены
- Release notes корректны

## Семантическое версионирование

Проект следует [Semantic Versioning 2.0.0](https://semver.org/):

- **MAJOR** (v1.0.0 → v2.0.0): Breaking changes
- **MINOR** (v1.0.0 → v1.1.0): Новая функциональность (обратно совместимая)
- **PATCH** (v1.0.0 → v1.0.1): Исправления ошибок

### Примеры

```bash
# Patch release (bug fixes)
git tag -a v1.0.1 -m "Fix connection timeout issue"

# Minor release (new features)
git tag -a v1.1.0 -m "Add support for SASL authentication"

# Major release (breaking changes)
git tag -a v2.0.0 -m "Redesigned configuration format"
```

## Pre-release версии

Для тестирования можно создавать pre-release версии:

```bash
# Alpha version
git tag -a v1.1.0-alpha.1 -m "Alpha release for testing"

# Beta version
git tag -a v1.1.0-beta.1 -m "Beta release for testing"

# Release candidate
git tag -a v1.1.0-rc.1 -m "Release candidate"
```

Workflow автоматически пометит такие релизы как "pre-release".

## Ручная сборка (локально)

Если нужно собрать бинарный файл локально:

```bash
# Для текущей платформы
go build -o kafka-producer-ui -ldflags="-s -w" .

# Кросс-компиляция для Linux
GOOS=linux GOARCH=amd64 go build -o kafka-producer-ui-linux-amd64 -ldflags="-s -w" .

# Кросс-компиляция для macOS (Apple Silicon)
GOOS=darwin GOARCH=arm64 go build -o kafka-producer-ui-darwin-arm64 -ldflags="-s -w" .

# Кросс-компиляция для Windows
GOOS=windows GOARCH=amd64 go build -o kafka-producer-ui-windows-amd64.exe -ldflags="-s -w" .
```

Флаги `-ldflags="-s -w"` уменьшают размер бинарного файла, удаляя отладочную информацию.

## Откат релиза

Если релиз содержит критическую ошибку:

1. Удалите тег локально и на GitHub:
   ```bash
   git tag -d v1.0.0
   git push origin :refs/tags/v1.0.0
   ```

2. Удалите релиз на GitHub вручную

3. Исправьте проблему и создайте новый тег

## Мониторинг workflow

Следите за выполнением workflow:
- [Actions](../../actions/workflows/release.yml)

При ошибках проверьте:
- Логи GitHub Actions
- Корректность тега (должен начинаться с `v`)
- Прохождение тестов

## Continuous Integration

На каждый push и pull request автоматически запускается CI workflow:
- ✅ Тесты на Linux, macOS, Windows
- ✅ Lint проверки
- ✅ Security scan
- ✅ Сборка для всех платформ

Убедитесь, что CI проходит перед созданием релиза.

## Требования

- Go 1.21+ (см. `go.mod`)
- Git
- Доступ к GitHub для push тегов

## Дополнительная информация

- [GitHub Actions Documentation](https://docs.github.com/en/actions)
- [Go Release Process](https://go.dev/doc/devel/release)
- [Semantic Versioning](https://semver.org/)

